apiVersion: v1
kind: Secret
metadata:
  name: ca-secret
  namespace: cert-manager
data:
  tls.crt: {{ lookup('file', '../certs/tls.crt') | b64encode }}
  tls.key: {{ lookup('file', '../certs/tls.key') | b64encode }}